#!/bin/bash

if [[ -e ~/.config/doom || -e ~/.doom.d ]]; then
    if [[ -e ~/.config/doom ]]; then
        git -C ~/.config/doom pull --rebase --autostash
    else
        git -C ~/.doom.d pull --rebase --autostash
    fi
fi
